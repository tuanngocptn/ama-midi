---
description: TypeScript and React coding standards for the project
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Coding Conventions

## TypeScript

- Strict mode enabled (`"strict": true` in tsconfig)
- Never use `any` -- use `unknown` and narrow, or define proper types
- Prefer `interface` for object shapes, `type` for unions/intersections
- Use `as const` for literal tuples and enums
- Import types with `import type { ... }` when only used as types

## React (apps/web)

- Functional components only, no class components
- Custom hooks (`use*`) for reusable logic -- colocate in a `hooks/` folder
- Components in PascalCase files: `PianoRoll.tsx`, `NoteEditor.tsx`
- Keep components small; extract sub-components when > 100 lines
- Use Zustand stores in `stores/` -- one store per domain (songs, notes, auth, ws)

## Hono API (apps/api) -- Route / Service Split

Routes are thin controllers. Business logic lives in services.

```
src/
  routes/          Thin controllers: parse input, call service, return JSON
  services/        Business logic: DB queries, access checks, error throwing
  middleware/      Auth, CSRF, rate limiting
  lib/             Utilities: JWT, password hashing
  db/              Drizzle schema
  durable-objects/ Durable Object classes (SongRoom for WebSocket)
```

- **Routes**: Parse request (Zod), call service, return `{ data: T }` or let error handler catch
- **Services**: Pure business logic, receive `db: DB` as first arg, throw `HTTPException` on errors
- **Access control**: Shared in `services/access-control.ts` (ensureAccess, ensureEditorAccess, ensureOwner)
- Group routes with `app.route('/prefix', subApp)`
- Always validate request body with Zod in the route before calling the service
- Return consistent JSON: `{ data: T }` on success, `{ error: string, code: string }` on failure

## Naming

| Thing | Convention | Example |
|---|---|---|
| Component files | PascalCase | `PianoRoll.tsx`, `NoteCard.tsx` |
| Non-component files | kebab-case | `note-service.ts`, `use-notes.ts` |
| Components | PascalCase | `PianoRoll`, `NoteCard` |
| Functions/vars | camelCase | `createNote`, `songId` |
| Constants | UPPER_SNAKE | `MAX_TRACKS`, `TOTAL_PITCHES` |
| DB columns | snake_case | `song_id`, `created_at` |
| API routes | kebab-case | `/api/songs/:id/notes` |

## Error Handling

- Never swallow errors with empty catch blocks
- API: throw `HTTPException` from Hono with proper status codes
- Frontend: surface errors to user via toast notifications
- Log errors with context (operation name, relevant IDs)

## Constants (packages/shared)

```typescript
const MAX_TRACKS = 8;
const MAX_TIME = 300;
const TRACK_RANGE = { min: 1, max: 8 } as const;
const TIME_RANGE = { min: 0, max: 300 } as const;

const TOTAL_PITCHES = 88;
const PITCH_RANGE = { min: 0, max: 87 } as const;

const CHROMATIC_SOLFEGE = [
  'Do', 'Do#', 'Re', 'Re#', 'Mi', 'Fa',
  'Fa#', 'Sol', 'Sol#', 'La', 'La#', 'Si',
] as const;
```

Helper functions: `pitchToMidi(pitch)`, `pitchName(pitch)`, `isBlackKey(pitch)`.
Pitch frequencies precomputed via equal temperament for Web Audio API playback.
